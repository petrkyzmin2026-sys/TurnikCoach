plugins {
    id 'com.android.application'
}

android {
    namespace 'ru.petr.messenger'
    compileSdk 35

    defaultConfig {
        applicationId 'ru.petr.messenger'
        minSdk 26
        targetSdk 35
        versionCode 100
        versionName '1.0.0'
    }

    buildTypes {
        debug {
            minifyEnabled false
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
