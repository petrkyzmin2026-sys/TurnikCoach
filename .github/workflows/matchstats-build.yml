name: Build MatchStats APK

on:
  push:
    branches: [ matchstats-prototype ]
    paths:
      - 'matchstats/**'
      - '.github/workflows/matchstats-build.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: android-actions/setup-android@v3
      - name: Install Android SDK
        run: sdkmanager 'platforms;android-35' 'build-tools;35.0.0'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'
      - name: Build debug APK
        working-directory: matchstats
        run: gradle :app:assembleDebug --no-daemon
      - name: Prepare DENZL APK
        run: cp matchstats/app/build/outputs/apk/debug/app-debug.apk DENZL-v0.9-independent-model.apk
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: DENZL-v0.9-independent-model
          path: DENZL-v0.9-independent-model.apk
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release delete denzl-v0.9-independent-model --yes || true
          git push origin :refs/tags/denzl-v0.9-independent-model || true
          gh release create denzl-v0.9-independent-model DENZL-v0.9-independent-model.apk --target "$GITHUB_SHA" --title "DENZL v0.9" --notes "DENZL probabilities are calculated independently from bookmaker odds. Market/API-Football is optional and used only for control comparison. SofaScore provides fixtures, pregame form and live statistics. Manual LIVE refresh retained."
